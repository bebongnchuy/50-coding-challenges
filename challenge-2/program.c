#include <stdio.h>

int main(){
    // Print the odd numbers less than 100
    int count = 100;
    for (size_t i = 0; i < count; i++)
    {
        if (i % 2)
        {
            /* code */
            printf("%d\n", i);
        }
        
        /* code */
    }

    return 0;
    
}